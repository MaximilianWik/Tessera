{
  "name": "tessera",
  "version": "0.1.0",
  "description": "Verified-permanent QR code generator. Built for codes that must work for life.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "serve": "npx --yes http-server -p 8080 -c-1 .",
    "test": "echo 'Tests run in the browser. Open tests.html.' && exit 0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/USER/tessera.git"
  },
  "dependencies": {},
  "devDependencies": {}
}
